<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qzc.mobiledlsearch">
    <!-- delete the application and install again, otherwise it not works -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".remote.RemoteDownloadListActivity"></activity>
        <activity android:name=".DownloadActivity"></activity>
        <activity android:name=".DownloadListActivity"></activity>
        <activity android:name=".EditableProgressActivity"></activity>
        <activity android:name=".SensorActivity"></activity>
        <activity android:name=".SparqlActivity"></activity>
        <activity android:name=".ProgressActivity" />
        <activity android:name=".SliderActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".ExpandingActivity" />
        <activity android:name=".SampleActivity"
                  android:screenOrientation="portrait"
                  android:theme="@style/AppTheme.NoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>