<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qzc.mobiledlsearch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".SparqlActivity">
        </activity>
        <activity android:name=".ProgressActivity"></activity>
        <activity android:name=".SliderActivity"></activity>
        <activity android:name=".ImageActivity"></activity>
        <activity android:name=".SpinnerActivity"></activity>
        <activity android:name=".ExpandingActivity"></activity>
        <activity android:name=".SampleActivity" android:screenOrientation="portrait" android:theme="@style/AppTheme.NoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

    <!--delete the application and install again, otherwise it not works-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>